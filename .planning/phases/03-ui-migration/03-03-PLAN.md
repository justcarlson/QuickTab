---
phase: 03-ui-migration
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - entrypoints/welcome/index.html
  - entrypoints/welcome/main.ts
  - entrypoints/welcome/style.css
autonomous: true

must_haves:
  truths:
    - "Welcome page displays hero image with WebP/PNG fallback"
    - "Welcome page shows feature explanation in FAQ card format"
    - "All user-visible text uses i18n strings from messages.json"
    - "Welcome page supports dark mode via system preference"
  artifacts:
    - path: "entrypoints/welcome/index.html"
      provides: "Welcome page HTML with hero and FAQ sections"
      contains: "hero-image"
    - path: "entrypoints/welcome/main.ts"
      provides: "Welcome page i18n population"
      contains: "chrome.i18n.getMessage"
    - path: "entrypoints/welcome/style.css"
      provides: "Welcome page styles with dark mode"
      contains: "prefers-color-scheme"
  key_links:
    - from: "entrypoints/welcome/index.html"
      to: "public/images/pages/welcome/hero-v2.webp"
      via: "picture source srcset"
      pattern: "/images/pages/welcome/hero-v2"
---

<objective>
Implement the welcome page with hero image, feature explanation, and FAQ cards.

Purpose: Provide new users an introduction to QuickTab on first install. Shows what the extension does and how to use it.

Output: Functional welcome page with hero illustration, FAQ cards, and consistent styling.
</objective>

<execution_context>
@/home/jc/.claude/get-shit-done/workflows/execute-plan.md
@/home/jc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ui-migration/03-CONTEXT.md
@.planning/phases/03-ui-migration/03-RESEARCH.md
@.planning/phases/03-ui-migration/03-01-SUMMARY.md
@public/_locales/en/messages.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create welcome page HTML structure</name>
  <files>entrypoints/welcome/index.html</files>
  <action>
Replace the placeholder HTML with the full welcome page structure.

Structure:
```html
<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to QuickTab</title>
  <link rel="stylesheet" href="./style.css">
</head>
<body>
  <main id="app">
    <header class="hero">
      <p id="thank-you" class="eyebrow"></p>
      <h1 id="headline"></h1>
      <p id="hook" class="lead"></p>
    </header>

    <div class="hero-image-container">
      <picture>
        <source srcset="/images/pages/welcome/hero-v2.webp" type="image/webp">
        <img src="/images/pages/welcome/hero-v2.png"
             alt="QuickTab routing a Zendesk link to an existing tab"
             id="hero-image"
             class="hero-image">
      </picture>
    </div>

    <section class="faq-section">
      <div class="faq-grid">
        <article class="faq-card">
          <h3 id="faq-1-title"></h3>
          <p id="faq-1-body"></p>
        </article>
        <article class="faq-card">
          <h3 id="faq-2-title"></h3>
          <p id="faq-2-body"></p>
        </article>
        <article class="faq-card">
          <h3 id="faq-3-title"></h3>
          <p id="faq-3-body"></p>
        </article>
        <article class="faq-card">
          <h3 id="faq-4-title"></h3>
          <p id="faq-4-body"></p>
        </article>
      </div>
    </section>

    <footer class="cta-section">
      <a href="https://github.com/zendesklabs/QuickTab#readme"
         target="_blank"
         rel="noopener noreferrer"
         class="cta-button"
         id="cta-button"></a>
    </footer>
  </main>
  <script type="module" src="./main.ts"></script>
</body>
</html>
```

Key elements:
- Hero section with eyebrow text, headline, and hook
- Picture element with WebP source and PNG fallback
- 2x2 FAQ card grid
- CTA button linking to docs
- All text elements have IDs for i18n population
  </action>
  <verify>
Run `npm run build` and verify HTML is valid.
Open welcome.html in browser and verify structure renders.
  </verify>
  <done>
Welcome page HTML contains hero, FAQ grid, and CTA with i18n placeholder elements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create welcome page styles</name>
  <files>entrypoints/welcome/style.css</files>
  <action>
Create welcome page styles that share the same design tokens as popup.

Content:
```css
/**
 * QuickTab Welcome Page Styles
 * Uses same design tokens as popup for consistency
 */

:root {
  /* Light mode palette */
  --bg: #f7f7f2;
  --surface: #ffffff;
  --text: #1b1d1a;
  --muted: #5a5f56;
  --border: #e5e6dd;
  --accent: #c8f05a;
  --accent-dark: #9fd138;
  --accent-soft: #eef7d6;
}

@media (prefers-color-scheme: dark) {
  :root {
    --bg: #111311;
    --surface: #1c1f1c;
    --text: #f2f4ef;
    --muted: #a4aba0;
    --border: #2e332d;
    --accent: #c8f05a;
    --accent-dark: #9fd138;
    --accent-soft: #20300f;
  }
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif;
  font-size: 16px;
  line-height: 1.6;
  color: var(--text);
  background: var(--bg);
}

#app {
  max-width: 800px;
  margin: 0 auto;
  padding: 48px 24px;
}

/* Hero section */
.hero {
  text-align: center;
  margin-bottom: 32px;
}

.eyebrow {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--accent-dark);
  margin-bottom: 12px;
}

h1 {
  font-size: 32px;
  font-weight: 700;
  line-height: 1.2;
  margin-bottom: 16px;
}

.lead {
  font-size: 18px;
  color: var(--muted);
  max-width: 560px;
  margin: 0 auto;
}

/* Hero image */
.hero-image-container {
  text-align: center;
  margin: 40px 0;
}

.hero-image {
  max-width: 100%;
  height: auto;
  border-radius: 12px;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
}

@media (prefers-color-scheme: dark) {
  .hero-image {
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4);
  }
}

/* FAQ section */
.faq-section {
  margin: 48px 0;
}

.faq-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
}

@media (max-width: 600px) {
  .faq-grid {
    grid-template-columns: 1fr;
  }
}

.faq-card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px;
}

.faq-card h3 {
  font-size: 14px;
  font-weight: 600;
  margin-bottom: 8px;
}

.faq-card p {
  font-size: 14px;
  color: var(--muted);
  line-height: 1.5;
}

/* CTA section */
.cta-section {
  text-align: center;
  margin-top: 48px;
}

.cta-button {
  display: inline-block;
  padding: 14px 28px;
  font-size: 14px;
  font-weight: 600;
  color: var(--text);
  background: var(--accent);
  border-radius: 8px;
  text-decoration: none;
  transition: background-color 0.15s, transform 0.15s;
}

.cta-button:hover {
  background: var(--accent-dark);
  transform: translateY(-1px);
}

.cta-button:active {
  transform: translateY(0);
}
```
  </action>
  <verify>
Run `npm run build` and verify no CSS errors.
Preview welcome page and verify:
- Hero section is centered with proper typography
- Hero image displays with rounded corners and shadow
- FAQ cards display in 2x2 grid on desktop, 1-column on mobile
- CTA button has accent color and hover state
- Dark mode colors apply when system is in dark mode
  </verify>
  <done>
Welcome page styles match design tokens from popup and support responsive layout with dark mode.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement welcome page TypeScript</name>
  <files>entrypoints/welcome/main.ts</files>
  <action>
Replace the placeholder with i18n population logic.

Implementation:
```typescript
/**
 * QuickTab Welcome Page
 *
 * Populates i18n strings for the welcome page content.
 * Shown to users on first install.
 */

/**
 * Get localized string with fallback to key name.
 */
function t(key: string): string {
  return chrome.i18n.getMessage(key) || `[${key}]`;
}

/**
 * Populate all i18n strings on the welcome page.
 */
function populateI18n(): void {
  // Hero section
  document.getElementById('thank-you')!.textContent =
    t('txt_welcome_header_thank_you');
  document.getElementById('headline')!.textContent =
    t('txt_welcome_header_main');
  document.getElementById('hook')!.textContent =
    t('txt_welcome_header_hook');

  // FAQ cards
  document.getElementById('faq-1-title')!.textContent =
    t('txt_welcome_header_what_do');
  document.getElementById('faq-1-body')!.textContent =
    t('txt_welcome_body_what_do');

  document.getElementById('faq-2-title')!.textContent =
    t('txt_welcome_header_new_browser_tab');
  document.getElementById('faq-2-body')!.textContent =
    t('txt_welcome_body_new_browser_tab');

  document.getElementById('faq-3-title')!.textContent =
    t('txt_welcome_header_lose_work');
  document.getElementById('faq-3-body')!.textContent =
    t('txt_welcome_body_lose_work');

  document.getElementById('faq-4-title')!.textContent =
    t('txt_welcome_header_what_links');
  document.getElementById('faq-4-body')!.textContent =
    t('txt_welcome_body_what_links');

  // CTA button
  document.getElementById('cta-button')!.textContent =
    t('txt_welcome_button_read_docs');
}

/**
 * Initialize welcome page on DOM ready.
 */
document.addEventListener('DOMContentLoaded', () => {
  populateI18n();
});
```
  </action>
  <verify>
1. Run `npm run build` and verify no TypeScript errors
2. Load extension in Chrome
3. Navigate to chrome-extension://[ID]/welcome.html
4. Verify all text content displays (no [key] fallbacks)
5. Verify hero image displays correctly
6. Verify CTA button links to GitHub README
  </verify>
  <done>
Welcome page displays with all i18n strings populated and hero image visible.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Welcome page displays hero image with proper WebP/PNG fallback
3. All text content shows localized strings (not placeholder keys)
4. FAQ cards display in 2x2 grid on desktop
5. CTA button links to GitHub README
6. Page respects system dark mode preference
7. Page is responsive on mobile widths
</verification>

<success_criteria>
- Welcome page displays hero image with WebP/PNG fallback
- Welcome page shows feature explanation in FAQ card format
- All user-visible text uses i18n strings from messages.json
- Welcome page supports dark mode via system preference
- CTA button opens documentation in new tab
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-migration/03-03-SUMMARY.md`
</output>
